import moment from 'moment';
import React from 'react'

export default function DOLineItem(props) {
    const { handle,data } = props;
    return (
        <h2 className={`doLineHorizontal`}></h2>
    )
}
